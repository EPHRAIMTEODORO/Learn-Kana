import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Research Alignment | Learn Kana',
  description: 'Map Learn Kana features to Smart Learning, Virtual TA, educational data mining, and adaptive UI research.',
};

export default function ResearchAlignmentLayout({ children }: { children: React.ReactNode }) {
  return children;
}
